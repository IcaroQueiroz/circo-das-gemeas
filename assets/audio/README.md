# Áudio ambiente

Coloque aqui o arquivo `ambient.mp3` (ou altere `audioSrc` em `js/convidados.js`).

A reprodução só é iniciada depois de uma interação do convidado, respeitando as políticas de autoplay dos navegadores móveis.
